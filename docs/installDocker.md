[back](../README.md)
### Install docker
Here we use `colima` as an alternative to `docker desktop` (due to licensing issues)

```
brew install docker
brew install colima
colima start
```
[back](../README.md)